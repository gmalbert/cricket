venv/
.venv/
__pycache__/
*.pyc
*.pyo
.env
*.code-workspace
.local/

# Local development and tool output
.commandcode/
.playwright-cli/
.pytest_cache/
.coverage
htmlcov/
*.log
pipeline_output.txt
logs_*.zip

# Pipeline audit history and rollback copies — generated locally; only the
# top-level cache/*.json files are published and committed by nightly CI.
cache/runs/
cache/backup/

# Large raw data files — downloaded nightly by the pipeline, not committed
cache/raw/

# Model binaries — re-trained each nightly run
cache/models/

# Cached JSON predictions ARE committed (nightly pipeline pushes them)
# cache/*.json  ← intentionally not ignored
